query userOrders($user: ID!) {
    user(login:$user) {
        displayName
        belongsTo {
            name
        }
        has {
            date
            items {
                quantity
                item {
                    name
                }
            }
            payedWith {
                type
            }
        }
    }
}
