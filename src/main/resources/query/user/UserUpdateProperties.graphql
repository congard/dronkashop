query UserProperties($id: ID!) {
    user(_id: $id) {
        displayName
        login
        email
        registrationDate
        belongsTo {
            _id
        }
        has {
            _id
        }
        sells {
            _id
        }
    }
}