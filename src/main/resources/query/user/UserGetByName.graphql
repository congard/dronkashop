query UserGetByName($displayName: String!) {
    user(displayName: $displayName) {
        _id
        displayName
        login
        email
        registrationDate
        # belongsTo
        # has
        sells {
            _id
        }
    }
}