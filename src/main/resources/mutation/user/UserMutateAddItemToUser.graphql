mutation UserMutateAddItemToUser(
    $itemId: ID!,
    $id: ID!
) {
    addItemToUser(
        itemId: $itemId,
        userId: $id
    ) {
        _id
        sells {
            _id
        }
    }
}
