mutation UserMutateRemoveItemFromUser(
    $itemId: ID!,
    $id: ID!
) {
    removeItemFromUser(
        itemId: $itemId,
        userId: $id
    ) {
        _id
        sells {
            _id
        }
    }
}
