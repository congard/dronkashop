mutation UserMutateRemoveOrderFromUser(
    $orderId: ID!,
    $id: ID!
) {
    removeOrderFromUser(
        orderId: $orderId,
        userId: $id
    ) {
        _id
        has {
            _id
        }
    }
}
