mutation UserMutateAddRoleToUser(
    $roleId: ID!,
    $userId: ID!
) {
    addRoleToUser(
        roleId: $roleId,
        userId: $userId
    ) {
        belongsTo {
            _id
        }
    }
}
