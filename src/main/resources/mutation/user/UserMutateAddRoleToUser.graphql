mutation UserMutateAddRoleToUser(
    $roleId: ID!,
    $id: ID!
) {
    addRoleToUser(
        roleId: $roleId,
        userId: $id
    ) {
        _id
        belongsTo {
            _id
        }
    }
}
