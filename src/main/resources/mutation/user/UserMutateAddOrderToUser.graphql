mutation UserMutateAddOrderToUser(
    $orderId: ID!,
    $id: ID!
) {
    addOrderToUser(
        orderId: $orderId,
        userId: $id
    ) {
        _id
        has {
            _id
        }
    }
}
