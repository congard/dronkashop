mutation UserMutateAddOrderToUser(
    $orderId: ID!,
    $userId: ID!
) {
    addOrderToUser(
        orderId: $orderId,
        userId: $userId
    ) {
        has {
            _id
        }
    }
}
