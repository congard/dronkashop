mutation UserMutateRelations(
    $id: ID!,
    $sellsAdded: [Int!],
    $sellsDeleted: [Int!]
) {
    mutateUserRelations(
        _id: $id,
        sellsAdded: $sellsAdded,
        sellsDeleted: $sellsDeleted
    ) {
        sells {
            _id
        }
    }
}