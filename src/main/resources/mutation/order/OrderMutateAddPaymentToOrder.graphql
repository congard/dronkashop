mutation OrderMutateAddPaymentToOrder(
    $paymentId: ID!,
    $orderId: ID!
) {
    addPaymentToOrder(
        paymentId: $paymentId,
        orderId: $orderId
    ) {
        payedWith {
            _id
        }
    }
}
