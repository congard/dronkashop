mutation addCategoryToItem($itemId: ID!, $categoryId: ID!) {
    addCategoryToItem(itemId: $itemId, categoryId: $categoryId) {
        _id
        name
        description
        belongsTo {
            _id
            name
        }
    }
}
