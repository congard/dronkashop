mutation ItemMutateAddCategoryToItem(
    $categoryId: ID!,
    $itemId: ID!
) {
    addCategoryToItem(
        categoryId: $categoryId,
        itemId: $itemId
    ) {
        belongsTo {
            _id
        }
    }
}