mutation CategoryMutateAddItemToCategory(
    $id: ID!,
    $itemId: ID!
) {
    addItemToCategory(
        categoryId: $id,
        itemId: $itemId
    ) {
        _id
        includes {
            _id
        }
    }
}