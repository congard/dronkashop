mutation CategoryMutateAddItemToCategory(
    $categoryId: ID!,
    $itemId: ID!
) {
    addItemToCategory(
        categoryId: $categoryId,
        itemId: $itemId
    ) {
        includes {
            _id
        }
    }
}